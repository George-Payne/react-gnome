// @flow

import 'utils/compat.js';

import render from 'render';

// Aliases for createElement method
const Window = 'WINDOW';
const Label = 'LABEL';
const Grid = 'GRID';
const Button = 'BUTTON';

export { render, Window, Button, Label, Grid };
